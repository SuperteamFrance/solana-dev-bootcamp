fn main() {
    // Déclarer ici la variable age

    // Déclarer ici la constante MAX_SCORE

    // Déclarer ici la variable temperature

    // Afficher toutes les valeurs
    println!("Votre âge : {}", age);
    println!("Score maximum : {}", MAX_SCORE);
    println!("Température : {}", temperature);
}